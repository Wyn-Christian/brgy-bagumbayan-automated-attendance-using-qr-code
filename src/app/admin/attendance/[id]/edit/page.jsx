import { CONFIG } from 'src/global-config';

// ----------------------------------------------------------------------

export const metadata = { title: `Edit attendance | ${CONFIG.appName}` };

export default function Page() {
	return (
		<div>Edit attendance</div>
	)
}
